rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 用戶文件：只允許本人讀寫（匿名用戶亦可，只要 auth.uid 對得上）
    match /users/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
